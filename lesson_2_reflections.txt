What happens when you intialize a repository? Why do you need todo it?
git creates a hidden folder. I need to do this so git knows what folders to manage.

How is the staging area different from the working directory and the repository? What value do you think it offers?
The staging area is a intermediary space where we can put the files that we wish to commit. It helps organize our work.

How can you use the staging area to make sure you have one commit per logical change?
If a file is in the staging area, it should be commited, representing a logical change.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
When i want to experiment a new functionality while still maintaining a working version of my program.

How do the diagrams help you visualize the branch structure?
It's easier to understand the branch structure through a diagram.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
The result is a new brach with both the changes from the two merged branches.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
pros: automatic merging is easier and faster.
cons: you have less control. Doesn't fix conflicts. 